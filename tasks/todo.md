# 订阅代理 V1 任务与验收

- [x] 核对旧文档、未提交草稿、Go版本及本地工具链状态。
- [x] 分派高级程序员开发兑换核心；限定仅修改 model/redemption.go 和相关测试。
- [x] 第一阶段实现与6个回归测试函数已写入，完成静态复查；下列测试与迁移验收仍未通过运行验证。
- [x] 第二阶段计划/实例策略与双额度快照、日重置和旧计费拒绝路径已写入；新增4个聚合测试，未运行。
- [x] 隔离验收脚本与环境说明已准备；生产机只读资源检查完成，未安装工具链或启动测试容器。
- [x] 第三阶段模型层已写入：请求账本、输入/输出预扣、结算、退款、重放防护和重置代次；4个新增测试函数尚未运行，未接HTTP请求链。
- [x] 增加服务层V1适配函数，明确传入原始输入/输出Token；尚未在中继主流程启用。
- [x] 增加V1管理员卡密生成接口`POST /api/redemption/subscription`和用户兑换接口`POST /api/subscription/redeem`；卡密仍由模型层绑定计划/服务并由唯一索引防重。
- [ ] 1. 兑换类型隔离与服务绑定。验收：钱包拒绝订阅/未知类型；订阅拒绝错误计划、服务、用户；代码审查与模型回归测试。
- [ ] 2. 卡密兼容及并发事务。验收：长度<=32、足够随机、重复兑换仅一次、失败回滚；真实数据库矩阵与竞态测试。
- [ ] 检查点A：记录已实现与实际运行结果，不把未执行测试标记通过。
- [ ] 3. 订阅计划/实例服务限制与每日输入输出配额设计；迁移新建和升级均验证。依赖1。
- [ ] 4. 渠道选择、预扣、结算、退款和跨日重置接入及端到端计费回归。依赖3。
- [ ] 5. 管理生成及用户兑换API、鉴权限流审计。依赖1、3、4。
- [ ] 6. 前端真实订阅状态、兑换、购买入口。依赖5。
- [ ] 检查点B：完整隔离验收、余额回归、故障退款、安全审查。
- [ ] 7. 真实成本对账与发布回滚准备；确认源码与生产定制功能兼容。

## 发布提案补充（尚未执行）

- [ ] R1 基线及完整发布提交。依赖7的兼容核对；验收：路由包含在提交、定制倍率与会话回归通过、镜像绑定提交与摘要。验证：差异审查、隔离回归。范围：小，路由/CI/发布清单。
- [ ] R2 管理员隔离入口。依赖检查点B；验收：独立数据库/网络/凭据、资源受限、非白名单后端拒绝。验证：普通账户直接访问及消费均拒绝，管理员完整兑换消费刷新通过。范围：中，Compose/Caddy/权限/测试配置。
- [ ] R3 兼容迁移及分批生产发布。依赖R1/R2；验收：备份恢复演练通过、开关默认关闭、全部副本兼容且在途流安全排空。验证：混合版本和迁移回归、普通钱包及流式冒烟、管理员灰度。范围：中，迁移/Compose/开关/发布清单。
- [ ] R4 销售放行与故障演练。依赖R3；验收：最坏成本核对、SKU卡型不串、停兑停新请求后可对账并保留账本。验证：退款/超时/重复兑换演练及小批次库存核验。范围：中，商品映射/对账/运维文档。

## 2026-09-24 本轮计划状态：管理员后台 UI 优化

- [x] 只读核对线上容器、镜像、挂载目录和管理员订阅路由。
- [x] 创建线上 UI/订阅源码回滚备份并记录 SHA256。
- [ ] 从已确认 Git commit 建立隔离构建目录。
- [ ] 服务端管理员权限直连验收：普通用户访问管理员订阅 API 必须 403。
- [ ] 订阅管理页改为圆角卡片布局，复用青蓝主题，移动端单列。
- [ ] 前端 lint、typecheck、build 和浏览器验收。
- [ ] 订阅扣费闭环接入并完成隔离数据库、竞态、流式失败测试。
- [ ] 单副本灰度、健康检查和回滚演练。

### 当前停止点

线上源码不是 Git 工作树，Go 测试工具和扣费主流程接入均未就绪。满足这些条件前不替换线上镜像、不开放套餐、不生成正式卡密。

## 2026-09-24 本轮计划状态：订阅事务测试超时修复

- [x] 复核 CI #1 的失败类型：订阅兑换回归测试因事务内另取 SQLite 连接而超时。
- [x] 修复并推送 `3dbcf7182660bd686ad9849577bd5af7c5ed1219`；将事务内数据库时间读取改为复用当前 `tx`。
- [x] GitHub Actions CI #3：后端 vet/build/test 和前端 typecheck/test 全部通过。
- [x] 香港 `/opt/newapi-v1` 干净工作区快进到同一提交；未构建镜像或触碰运行容器。
- [x] 复核事务时间 helper、所有事务调用点及订阅重放/结算断言；确认事务路径均传入当前 `tx`，回归用例覆盖兑换、预扣重放、结算和重复结算。
- [x] 为 `TestSubscriptionV1ReserveSettleAndReplay` 添加 `go test -race` CI 关卡。
- [x] 推送 workflow 变更；CI #4（SHA `d5a4af5464054eb82d1bc9d98be551272622b7ef`）race 回归1分16秒、全量后端55秒、前端165个测试文件/2065项均通过，总计6分34秒。
- [x] 记录未覆盖范围：当前 Actions 没有 MySQL/PostgreSQL 服务矩阵；不得将 SQLite 测试结果表述为生产数据库验证。

执行边界：仅 GitHub Actions runner；禁止操作生产数据库、容器、域名及镜像。保留四个未提交前端文件，不纳入本次提交。

## 后续计划状态：MySQL/PostgreSQL 单连接事务验证

- [x] 新增 `TestGetDBTimestampFromTransactionWithSingleConnection`，覆盖 SQLite、MySQL、PostgreSQL，pool 限制为1并检查 SQL 错误/上下文超时。
- [x] workflow 增加 CI 临时 MySQL 8、PostgreSQL 16 服务，DSN 仅提供给聚焦测试步骤。
- [x] CI #5（SHA `fc30bf35cb030245f575acfa3645c6ca1ebc3ce0`）聚焦 SQL 方言测试通过；workflow 本步骤提供 MySQL/PostgreSQL DSN，SQLite 子用例本地运行，因此三种方言均被纳入执行。
- [x] CI #5 race 回归、全量后端与前端均通过；前端165个测试文件、2065项通过，总耗时6分10秒。
- [x] 记录验收边界：仅验证临时数据库的数据库时间查询与单连接事务行为；未验证生产数据迁移、生产镜像或实际业务副本，不触碰香港生产数据库、容器和镜像。
- [ ] 将 CI 临时 MySQL/PostgreSQL DSN 暴露给 `make test`，执行仓库已有的跨库迁移/数据库行为测试并确认通过。
